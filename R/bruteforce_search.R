
bruteforce_search <- function(POI,
                              method, lavModel,
                              N_start = nrow(lavModel)*10,
                              Ntotal = 1000,
                              power_aim = .8, alpha = .05, lb = nrow(lavModel),
                              ...)
{
     dotdotdot <- list(...)

}
